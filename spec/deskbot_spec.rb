# frozen_string_literal: true

RSpec.describe Deskbot do
  it "has a version number" do
    expect(Deskbot::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
